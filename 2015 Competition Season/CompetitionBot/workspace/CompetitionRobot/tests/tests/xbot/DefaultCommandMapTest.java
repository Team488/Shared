package tests.xbot;

public class DefaultCommandMapTest extends BaseTestCase {

    //This is a bad test, and breaks whenever we change the default.
//	@Test
//	public void testDefaultCommandRuns() {
//		// run the scheduler
//		Scheduler.getInstance().run();
//		
//		((MockJoystick)injector.getInstance(OI.class).leftJoystick).setY(1.0);
//		
//		Scheduler.getInstance().run();		
//		
//		assertEquals(1.0, injector.getInstance(DriveSubsystem.class).leftFrontDrive.get(), 0.001);
//	}
}
